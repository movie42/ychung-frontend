{
  "name": "react-yangchung-build",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client-build": "cd client && npm ci && npm run build && cd ../",
    "server-build": "cd server && npm ci --dev && npm run build && cd ../",
    "heroku-prebuild": "npm run client-build && npm run server-build && mv ./client/build ./server/dist",
    "dev:server": "cd server && npm run dev:server",
    "dev:client": "cd client && npm start",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "start": "cd server && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd client && npm ci && cd ../ && cd server && npm ci"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}
